name: Keep Render API Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Lexicool API
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://api-lexicool.onrender.com/ping
